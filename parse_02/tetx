cmd				cat "main.c"|wc|ls>'la'|ls|wc

				cat "main.c"|wc|ls>'la'|ls|wc

				cat "main.c" | wc | ls>'la' | ls | wc 

		      cat "main.c" | wc | ls > 'la' | ls | wc

		   cat main.c | wc | ls > la | ls | wc

ok


			cat "main.c""|"



1 -> split with spaces but if it the word starts with '"' or '\'' split by space after
	find her close (another '"' or '\'')
					if (there another quote continue count the word)


2 --> split with pipes but again if it the word starts with '"' or '\'' split by pipes after
	find her close (another '"' or '\'')


3 ---> split with redirections but again (...) 


4 ----> hanlde the quotes 
			if {"a","b"}   {a, b}
			if {"a""b", ...}  [ab]
			if {ma"in".c, ...}    {}

				test  -->   cat "mai"n".c"
							cat 'mai"n".c'
							cat "mai'n'.c"
							cat ""mai'n'.c""
				
				""mai'n'.c"" ----> ""   mai   'n' -> n   .c    ""
								-->   main.c
				""   //try stack;


"" ""/*************
		"<" main.c cat
		if between (" ") --> cmd or arg;		
*****************/


		man"."c   limi = 0;
		ft_push(m)
		ft_push(a)
		ft_push(n) 
		 			limi = ' " '
		ft_push(.)
					limi = 0;
		ft_push(c);

ok  what if ("<"   &&  <   without quotes ) ?
	we need queue from the part 3;	ok // later

5 how the cmd should be in 

	cmds are splitted by '|'
		first word if its token ---> which type;                          darori first word ?
							if its pipe --> syntax error
							if its ---> '<' --> what fol is the input
							if --->  ' > '  --->  whats fol is the out
							if --->  '<<' ---> what is fol is limiter; later 
							if --->   
		

problem to this moment :
							if redirections between quotes;
							print syntax error if   pipe first or last
												if redirections without file;
							heredoc
							$		this should expand to the value 


